
services:
  backend:
    build: .
    ports:
      - "5001:5000"  # External : Internal (you'll access via localhost:5001)
    environment:
      - DATABASE_URL=postgresql://knowledgebase_9hh8_user:R7wDFCMGb9XVeXJgp6Zl9faiXBGGzbYR@dpg-d21oknnfte5s73fsi66g-a/knowledgebase_9hh8
      - JWT_SECRET=your_jwt_secret
    volumes:
      - .:/app
    command: npm run dev  # You can keep this here or in Dockerfile CMD
